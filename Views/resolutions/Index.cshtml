@model IEnumerable<Project.Models.resolution>

@{
    ViewBag.Title = "Index";
}

<h2>Problems</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.resID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Topic)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Details)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td onmouseover="onIdMouseOver(@item.resID)">
                @Html.DisplayFor(modelItem => item.resID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topic)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.resID }) |
                @Html.ActionLink("Details", "Details", new { id = item.resID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.resID })
            </td>
        </tr>
    }

</table>

@section Scripts{


    <script type="text/javascript">
        function onIdMouseOver(resID) {
            $.ajax({
                url: "/api/resolutionsAPI",
                data: {
                    resID: resID
                },
                success: function (result) {
                    console.log(result);
                    alert(JSON.stringify(result));
                }

            });

        }
    </script>



}